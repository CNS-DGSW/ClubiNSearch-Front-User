export interface IErrorValue {
  name: {
    context: string;
    isError: boolean;
  };
  schoolNumber: {
    context: string;
    isError: boolean;
  };
  phoneNumber: {
    context: string;
    isError: boolean;
  };
  introduce: {
    context: string;
    isError: boolean;
  };
  link: {
    context: string;
    isError: boolean;
  };
}
