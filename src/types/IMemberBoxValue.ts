export interface IMemberBoxValue {
  title: string;
  member?: IMemberValue[];
}

export interface IMemberValue {
  name: string;
  schoolNumber: string;
  phoneNumber: string;
  introduce: string;
}
